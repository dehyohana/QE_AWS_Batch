name: Build and Deploy

on:
  push:
    branches:
      - dev

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    env:
      ECR_REPOSITORY_NAME: ${{ env.ECR_REPOSITORY_NAME }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print repository environment variable
        run: echo "Value of ECR_REPOSITORY_NAME is $ECR_REPOSITORY_NAME"

      # Add your build and deploy steps here
